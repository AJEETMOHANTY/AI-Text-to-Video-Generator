# AI Text-to-Video Generator

This project demonstrates how to generate videos from natural language prompts using a diffusion-based generative model.
The system converts a text description into a sequence of video frames, which are then compiled into a playable video file.

Example prompt:
```bash
A cat walks on the grass, realistic
```

Output:
```bash
output.mp4
```